{
  "name": "biff-scheduler",
  "version": "0.1.0",
  "private": true,
  "description": "BIFF 电影节排片工具 - Cloudflare Pages + D1 + Vite/TS",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "build": "npm run typecheck && vite build",
    "preview": "npm run build && wrangler pages dev dist",
    "migrate:remote": "wrangler d1 migrations apply biff-scheduler-db --remote",
    "deploy": "npm run build && wrangler pages deploy dist --project-name biff-scheduler --branch production --commit-dirty=true"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.3.3",
    "tailwindcss": "^4.3.3",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "wrangler": "^4.0.0"
  }
}
