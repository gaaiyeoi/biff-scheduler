name = "biff-scheduler"
compatibility_date = "2026-09-09"
pages_build_output_dir = "./dist"

# 纯静态部署(2026-09-11,PLAN-20260911001107):D1 已整体退役 ——
# 豆瓣映射改由静态产物 `public/douban.json` 承载,片单只落浏览器 localStorage。
# 故本文件**不再有任何绑定**(无 d1_databases / 无 functions 路由),
# `wrangler pages deploy dist` 不需要任何后端资源。
