# PLAN 2026-09-10 01:16:07 — 图例「ⓘ 日程表说明」去掉点状虚线下划线

> 需求(用户原话):「日程表说明 这个地方组件的 外面的虚线很丑」。
> AskUserQuestion 澄清:虚线位置 = **首页网格图例里「ⓘ 日程表说明」文字下方的点状虚线**(非弹层内 KN 徽章框)。
> 本文件 = 本需求唯一规划文档。

## 现状(代码事实,HEAD d5b5d05 + 工作区在途)

- `index.html` 网格图例行尾(45–51 行容器内):
  `<span id="abbr-help" class="cursor-help border-b border-dotted border-muted hover:text-ink hover:border-biff" ...>ⓘ 日程表说明</span>`
  — 文字下方一条 dotted 下划线(伪装成"可点击链接"样式),与同排其它图例项(色块 + 文字,均无下划线)视觉语言不统一,观感突兀。
- 交互已由两套机制承担:① hover `data-tip` 弹大段字段总览(main.ts boot 装配,单源自 badges.ts abbrTooltip);② click 打开「排片表说明 · 图例总览」弹层。下划线并非可点性唯一提示。
- **工作区注意**:并行需求(GV 拼接卡 + 红绿灯底色,PLAN-20260910010232 / 010648 / 011409)实现仍在工作区未提交,`npm run typecheck` 现报 6 错(agenda.ts:6、main.ts:9/361 等,均为 GV 功能导入未接线),build/deploy 被阻塞——**与本需求无关**;本需求不碰那些文件。

## 方案

- 去掉 abbr-help 的 `border-b border-dotted border-muted` 与 `hover:border-biff`(下划线整套);hover 反馈改文字变色 `hover:text-biff`(主题红,与弹层内 summary hover 同语言),默认色继承容器 muted。`cursor-help` 保留。
- 不做成实线边框按钮 / 不加重样式 —— 保持图例行"纯文本可点"的轻量观感。

## 改动清单

- `index.html` 单行:
  - `class="cursor-help border-b border-dotted border-muted hover:text-ink hover:border-biff"` → `class="cursor-help hover:text-biff"`

## 验收

1. 视觉:图例行文字下方不再有点状虚线;hover 文字变红;点击/悬停说明弹层与 data-tip 均不受影响。
2. `npm run typecheck` 错误集合与改动前一致(仅并行在途的 GV 6 错,本需求不新增、不改动任何 TS)。
3. 本需求改动与并行在途改动同文件(index.html)同区域 → 提交时无法以文件粒度拆分,与并行需求收尾后统一提交/部署。

## 提交与部署

- 受并行在途 GV 需求(typecheck 6 错)阻塞,本改动先落工作区,待并行需求收尾、typecheck 归零后统一 `git add` + commit + push + `npm run deploy`(部署需在 typecheck 通过后进行)。
