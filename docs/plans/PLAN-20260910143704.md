# PLAN-20260910143704 — 回退「加入场次强制定档」（撤销 PLAN-20260910140904）

## 用户诉求

> 请你把这个改动去掉吧，排片不需要选择必看/备选/随缘，只需要在影片库添加到我的选片中需要必选。

即：`PLAN-20260910140904`（`58d8af9` + `e19bc93`）做过头了。**排片（网格点选场次）不该弹「选择档位」、
不该强制要求档位**；档位只在「影片库 → 添加到我的选片」这一个入口需要必选（该入口本来就有 seg，
点当前档 = 取消，语义不变）。

## 口径确认（`AskUserQuestion`）

> 去掉强制定档后，网格里点选一部还没在影片库定过档的片 —— 它应该出现在「我的行程」里吗？

用户答：**照常显示**（即完全回到改动前的行为）。

推论：**本次是「完全回退」，不做任何替代方案。**

| 项 | 回退后 |
|---|---|
| 网格点选未打标的片 | 直接加入，`priority = null`，**不弹层** |
| 行程里该场次 | **照常显示**；三段 seg 全灰 + 左侧一枚「未设」小字 |
| 档位语义 | 回到「影片级，可选；null = 未设」 |
| 影片库 → 添加到我的选片 | 仍必选档位（该入口的既有行为，未受本次影响） |

## 回退清单（严格对称于 58d8af9 / e19bc93）

| 文件 | 回退内容 |
|---|---|
| `src/main.ts` | ① import 去掉 `openPriorityPicker`；② 删 `pickScreening()` 整块；③ `filmModalCtx().toggle` 回 `toggleScreening`；④ GV 谈块入口回 `toggleScreening(key, code, store.picks.get(key)?.priority ?? null); setGvTalk(code, false);`；⑤ 网格卡入口回 `toggleScreening(key, code, store.picks.get(key)?.priority ?? null);` |
| `src/state.ts` | ① `loadPicks()` 末尾删 `pruneUnsetPicks()`；② `syncFromCloud()` 删 `pruneUnsetPicks()`；③ `setWish` 恢复「`!priority && picks.length === 0 && !note` 才删整条」（有场次 → 保留为「未设」）+ 恢复原注释；④ `toggleScreening` 删「一并定档」那半边，回 `commit(key, { ...cur, picks })`；⑤ 删 `pruneUnsetPicks()` 函数整块 |
| `src/agenda.ts` | ① 注释回「priority=null = 未设档位 …… 未分级不参与质量分」；② seg on 态 title 回 `取消「${label}」→ 回到未设(不参与质量分)` |
| `src/pick.ts` | `buildWishSeg` on 态 title 回 `(再点取消打标)` |
| `src/modal.ts` | ① `import type` 去掉 `Priority`；② `import { buildWishSeg }` 去掉 `PRI_BG_ON, WISH_ORDER`；③ 删 `openPriorityPicker()` 整块 |
| `docs/CONVENTIONS.md` | 「移除场次语义」条恢复「仅当「档位未设 + 无备注 + 最后一场」才整条删」；删掉新增的「「未设档位」= 非法状态」整段 |

## 执行方式

- 5 个源文件相对 HEAD **无在途改动**（并行会话在改的是 `venues.json` / `legend.ts` / `library.ts` /
  `types.ts` / `extract_schedule.py`），故直接改工作树 + `git add`。
  - `main.ts` 因 import 区被并行会话动过（`194b419` 加 `labelMetrics`、`f40825b` 加 `./ai`），
    反向 patch 上下文对不上 → 改用定点字符串替换（`/tmp/qa/revert_main.py`，5 处，每处断言唯一匹配）。
  - 其余 4 个文件走 `git apply -R`（`/tmp/qa/fwd-src.patch`，排除 `main.ts`）。
- `docs/CONVENTIONS.md` 是**混合文件**（并行会话在途 +10 行 venue `short`）→ 走 blob 手术
  （SKILL `parallel-agent-safe-commit`：`git show HEAD:` + `hash-object -w` + `update-index --cacheinfo`）。
  **工作树也同步回退**（否则并行会话下次 `git add` 会把我的回退又带回来）。
- 部署走**隔离 worktree**（共享树有并行 WIP）。

## 验收标准

1. `npm run typecheck` 通过（在 `HEAD + 本次回退` 的隔离快照上跑，不含并行 WIP）。
2. 三重断言（`/tmp/qa/audit_revert.py`）：① 我的标记全消失；② 回退目标原文全回来；③ orphan = 0
   （`CONVENTIONS.md` 的 orphan 必须恰好 = 对方在途那 10 行）。
3. `git diff --cached --stat` 只列本次 6 个文件 + 本 PLAN + 旧 PLAN 的「已回退」标注。
4. 线上核对（cache-buster）：`grep` 产物 JS 里 `pickScreening` / `openPriorityPicker` **不存在**，
   `toggleScreening` 存在。
5. 云端数据不恢复 —— 用户已接受「未设」重新出现；第一轮被 `pruneUnsetPicks()` 清掉的那 2 条
   （`cat:f033`、`sched:115, 160, 163, 164 midnight passion 3`）**不回填**。
