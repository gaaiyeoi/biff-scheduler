# PLAN 2026-09-12 20:43 — 排片表 / 时间线加海报与评分,详情加简介

> 需求(用户原话):
> ①「直接在排片表上加上海报，评分，评分人数，点击进入详情后，还要展示简介。」
> ②「时间线里也同样增加」
> 本文件 = 本需求唯一规划文档。

## 1. 数据(已有 vs 要补)

| 字段 | 现况 | 本轮 |
|---|---|---|
| 海报 | `films.json` 156/246 有本地 `/posters/…`;缺图不占位、不外链 | 网格卡 / 时间线卡直接用 |
| 评分 / 人数 | `films.json` 49 部有;`douban.json` 也有,但 `loadDoubanMappings` **丢掉了** | 映射把这两列读回来;展示优先目录,缺则映射 |
| 简介 | 豆瓣详情 `intro` 从未落盘 | 新产物 `public/douban-intros.json`(按 subject_id);缺文件弹层不占位 |

2026 新片大多还没分,经典复映才有(如《天使之卵》8.1 / 1.6万)。有就画,没有不加空壳。

简介走 Frodo `GET /api/v2/movie/{id}`,风控口径与映射管线相同(`103/1005/1309` 停轮)。

## 2. 排片表(二维网格)

卡片改成横向:`[海报 | 正文]`,无海报不留空列。
短场次(卡宽 < 130px)或缩到 55%/70% 行高时藏海报,避免把 CODE / 时间挤没。
评分用既有 `doubanChip`,补人数:`豆 8.5 1.6万`。贴在片名行右侧,先截片名。

几何签名不改(海报不是排期几何),点选仍走 patch。

## 3. 时间线

外壳加左缘海报(44×62,与影片库缩略图同尺寸);评分章进场次行流式容器(与等级 / 字幕同一条流,不新开一排)。
`screeningRow` 增可选 `chip`,影片库展开的场次行不传(同片重复贴海报没意义)。

## 4. 详情弹层

片名 / 海报 / 评分下方追加简介(豆瓣 `intro`)。目录片弹层同样。无简介不占位。

## 5. 验收

- 单测:`fmtVoters`(16019→1.6万);`doubanScoreOf` 目录优先、映射兜底、0 分当无
- 无头:开幕片网格卡能看到海报或评分(有数据才断言有);点 ⓘ 弹层里有简介或相关区仍在
- 窄屏时间线卡同样出现海报 / 评分章
