# PLAN-20260910105429 · 清理字面 `**` Markdown + 合并后覆盖推送部署

## 1. 需求

上一轮 `PLAN-20260910103258`(总览字段元素真章化)完成后,并行会话在同一工作区提交了
`047cddf`(「我的行程」与「我的选片」合并为单一数据源)。用户指令:

> 等他那边改完了你再改 然后再覆盖推送到 github 和 cloudfare

即:等并行会话收工 → 我方接手收尾改动 → 重新推送 GitHub + 覆盖部署 Cloudflare。

## 2. 现状(根因)

并行会话 `047cddf` 在重写文案时写入了 Markdown 粗体语法,但全站文案走的是
`textContent`(非 Markdown 渲染器),`**` 会被**原样显示成星号**。

全仓 `**` 排查结果:`src/` 下绝大多数 `**` 都在 JSDoc 注释里(无害),
只有 2 处落在**用户可见的字符串字面量**中:

| 文件:行 | 原文 | 呈现处 |
| --- | --- | --- |
| `src/legend.ts:450` | `档位是**影片级**的` | 「排片表说明 · 图例总览」§7 表格行 |
| `src/main.ts:530` | `**全部已排场次**` | 设置里「清空全部已排场次(A+B)」的 `window.confirm` |

## 3. 方案

统一改为全站既有的 `「…」` 引号写法(与同段落里的「影片库」「未设」等一致):

- `src/legend.ts`:`档位是「影片级」的`
- `src/main.ts`:`确定清空 A/B 两个方案的「全部已排场次」?`

仅 2 处字面替换,不动任何逻辑/结构/样式。

## 4. 不做

- 不改 `**` 所在的 JSDoc 注释(注释里 `**` 是合法 Markdown 强调,且不面向用户)。
- 不重构并行会话引入的 `store.picks` 单一数据源(非本需求范围)。
- 不动工作区里并行会话的未跟踪产物 `tools/extract_schedule.py`。

## 5. 验收

1. 全仓检索「字符串字面量内含 `**`」→ 0 命中(含双引号 / 单引号 / 模板串三类)。
2. 我上轮的总览字段真章化改动仍完整(`timeField` / `codeField` / `blankChip` /
   `swatch` / `labeled` / `doubanChip` / `BADGE_DEFS` 渲染全在位)。
3. 豆瓣章单一来源未被并行会话重写回内联(`doubanChip` 仍是唯一实现)。
4. `npm run build` 通过(tsc 无类型错误)。
5. 部署后线上核对:HTML 引用的 JS/CSS 哈希为新产物,且产物内含新文案
   「档位是「影片级」的」,不含 `档位是**影片级**`。

## 6. 提交与部署

- 只 `git add` 本 PLAN + `src/legend.ts` + `src/main.ts`(工作区另有并行会话的
  `M docs/plans/PLAN-20260910095800.md` 与 `?? tools/extract_schedule.py`,不纳入)。
- `git commit` → `git push`。
- 部署用隔离工作树(`git worktree add --detach <本 commit>` + 软链 `node_modules`),
  避免把并行会话在途文件卷进 `vite build`。

## 7. 实现纪要(回填)

- 收工判定:看门狗按「所有源码/管线文件最后写入时间」判定,连续静默 150s 触发;
  实测对方最后写入 `tools/extract_schedule.py` 于 10:51:04,HEAD 停在 `047cddf`。
- 改动落地:`src/legend.ts:450`、`src/main.ts:530` 各 1 处字面替换(共 2 行)。
- 合并安全性复核:我上轮的总览字段真章化(`timeField`/`codeField`/`blankChip`/
  `swatch`/`labeled`/`doubanChip`/`BADGE_DEFS` 渲染)在 `047cddf` 合并后**完整保留**;
  对方只重写了 §7 段,与我的 §1/§3/§4/§6 不相交。豆瓣章单一来源也未被重写回内联
  (`doubanChip` 仍是唯一实现,调用方 `library.ts`×2 / `modal.ts`×2 / 总览×1)。
- 验收结果:
  - 字符串字面量内含 `**` → 0 命中(双引号 / 单引号 / 模板串三类全查)。
  - `npm run build` 通过(tsc 无错);新产物 `index-DlwrMDl7.js` / `index-DcG10HrY.css`。
  - 构建产物核验:含「档位是「影片级」的」「「全部已排场次」」,不含旧星号文案。
- 提交与部署:
  - commit `6c7c000`(3 files,+65/−2),push `047cddf..6c7c000`。
  - 部署走隔离工作树(`git worktree add --detach /tmp/deploy-copy 6c7c000` +
    软链 `node_modules`),避免把并行会话在途文件卷进 `vite build`。
  - 部署成功:`https://c72a4c5e.biff-scheduler.pages.dev`。
  - 线上核对:生产域 `https://biff-scheduler.pages.dev/` 引用
    `./assets/index-DlwrMDl7.js` + `./assets/index-DcG10HrY.css`;
    线上 JS 含新文案、不含旧星号文案。隔离树已 `worktree remove --force` 清理,
    主工作区对方的 `M PLAN-20260910095800.md` / `?? tools/extract_schedule.py` 未受影响。
