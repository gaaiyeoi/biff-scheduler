# 2026-09-09

## biff-scheduler「一键查询豆瓣」历史排查(23:50)

用户反馈:印象里项目有个「一键查询豆瓣」组件,现在不见了,让查历史。

排查结论(证据链):
- 项目无 git(非仓库)、无旧 dist 备份、.workbuddy/memory 为空、历史会话检索无豆瓣相关内容 → 无代码级残留可找回。
- PLAN.md 白纸黑字:豆瓣自设计起就是「半自动」(§0 决策表 line 17:不做爬虫/防 CAPTCHA,前端拼搜索链接 + 用户粘贴回填;§5.1 同)。
- 落地偏差记录 #3(PLAN line 271):今天曾实测豆瓣官方 v2 API(goddlts 文档镜像)→ subject 403 / search invalid_apikey(104) → 放弃自动查询路径,维持半自动。
- 唯一仍开放的豆瓣匿名口 = `movie.douban.com/j/subject_suggest`,仅用于离线富化 tools/enrich_douban.py(海报/评分/中文名),不在前端。

当前豆瓣 UI 现状(未消失,仍在):
- 影片详情弹层(ⓘ/详情入口)→ 豆瓣区:已关联→直链;未关联→「点这里查豆瓣」中文/英文两个 douban.com/search 外链 + 粘贴链接+中文名「保存映射」→ PUT /api/mapping/:code → 服务端正则抽 subject_id 落 D1 douban_map。
- 影片库「详情 ⓘ」同弹层;agenda 行程行显示「已关联豆瓣」;ics 导出带豆瓣链接;films.json 50/250 有 rating → 卡片/详情「豆 x.x」徽章。

给用户的建议方向(待用户拍板):若想要真·一键自动查豆瓣并回填,可基于 subject_suggest 用 Pages Function 代理 + 候选点选落映射重建(注意限流/UA/缓存);或保持现状。建议给项目补 git,避免再发生代码版本丢失。
