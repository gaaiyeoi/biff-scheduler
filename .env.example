# Copy to .env in this directory (gitignored) or export in your shell.
# Never put real values in files that get committed.

# TMDB v4 Read Access Token. Used as: Authorization: Bearer <token>
# https://www.themoviedb.org/settings/api
TMDB_KEY=
